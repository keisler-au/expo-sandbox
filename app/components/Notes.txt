So, here we are, once again, let's go. I really want to get going. 

const headerCardY = useShareValue(50)
const gridSetY = data.keys().map((_, index) => ( useDerivedValue(() => {
    return headerCardY < 1 ? withTiming(gameIndex * (GRID_CONTAINER_SIZE + GRID_CONTAINER_GAP), { duration: 500 }) : 0;
    })
));
const gridSetOpacity = useDerivedValue(() => {
    return headerCardY.value < 1 ? 1 : 0;
});

So the carousel is simple. Keep that. 
* renderItem --> <Touchable><EmptyGridTemplate><Text>Social</Text>


onPress
* expanded = True
* headerCardY.value = withTiming(0, {duration: 500})


So the question is, once onPress is activated and expanded = True, what finishes first, does the state of expanded get updated and the components rendered, or do the SharedValues and DerivedValues get updated first? I don't think it matters, as long as by the time headerCard gets to the top the components are rendered, then they'll just apply whatever the animatedStyles are at that time





The question is now do I want to use the gridSetY